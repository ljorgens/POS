<h1>Cart</h1>

<ul>
  <% @new_purchase.products.each() do |product| %>
    <li><%= product.description() %> <%= product.price() %></li>

  <% end %>
</ul>
<h3>Total: $<%= @new_purchase.total_price() %></h3>
